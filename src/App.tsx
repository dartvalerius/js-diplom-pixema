import React, { useState } from 'react';
import { Search } from './components';

function App() {
  return (
    <div className="App">
      <Search onChange={() => {}} onClickFilter={() => {}} filtered/>
    </div>
  );
}

export default App;
